/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com',"images.openfoodfacts.org", "avatars.githubusercontent.com"], 
  },
};

module.exports = nextConfig;
